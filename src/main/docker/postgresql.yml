version: '2'
services:
  stock-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/stock/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=stock
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
